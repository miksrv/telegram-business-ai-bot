[project]
name = "telegram-business-ai-bot"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "pyTelegramBotAPI>=4.18.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
